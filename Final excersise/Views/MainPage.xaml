<Page
    x:Class="Final_excersise.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Final_excersise"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.TopAppBar>
        <CommandBar>
            <CommandBar.Content>
                <AppBarButton Name="home" Icon="Home" Label="Home"/>
            </CommandBar.Content>
            <AppBarButton Name="share" Icon="World" Label="Share"/>
            <AppBarButton Name="settings" Icon="Setting" Label="Settings"/>
        </CommandBar>
    </Page.TopAppBar>
    <ListView ItemsSource="{Binding Articles}" 
              Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
              x:Name="ArticlesListView"
              IsItemClickEnabled="True"
              ItemClick="ArticlesListView_OnItemClick">
        <ListView.ItemTemplate>
            <DataTemplate>
                <Grid Margin="0, 10, 0, 10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="125"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="125"/>
                    </Grid.RowDefinitions>

                    <Image Grid.Row="0" 
                           Grid.Column="0" 
                           Source="{Binding Image}" 
                           Stretch="UniformToFill" />
                    <StackPanel Grid.Row="0" 
                                Grid.Column="1"
                                Padding="20, 0, 10, 0">
                        <TextBlock Text="{Binding Title}" 
                                   FontWeight="Bold"/>
                        <TextBlock Text="{Binding Summary}" 
                                   TextWrapping="Wrap"
                                   TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </Grid>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</Page>

