<Page
    x:Class="Final_excersise.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Final_excersise"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <SplitView x:Name="MainSplitView"
               DisplayMode="CompactOverlay"
               IsPaneOpen="False"
               CompactPaneLength="0"
               OpenPaneLength="150">
        <SplitView.Pane>
            <StackPanel Background="#333">
                <Button x:Name="settingsClose" Width="150">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <SymbolIcon Symbol="Back" />
                            <TextBlock Text="Instellingen"></TextBlock>
                        </StackPanel>
                    </Button.Content>
                </Button>
                <StackPanel Padding="10">
                    <TextBlock x:Uid="settings.feedDescription" 
                               Text="Toon artikelen uit deze feeds:"
                               TextWrapping="Wrap"></TextBlock>
                    <ListView ItemsSource="{Binding Settings.Feeds}">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox IsChecked="{Binding IsChecked}"/>
                                    <TextBlock Text="{Binding Name}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
                
            </StackPanel>
        </SplitView.Pane>
        <!-- Main content -->
        <SplitView.Content>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal"
                            Grid.Row="0">
                    <Button x:Name="HamburgerButton" FontFamily="Segoe MDL2 Assets" Content="&#xE700;"
                            Width="50" Height="50" Background="Transparent"/>
                </StackPanel>
                <ListView ItemsSource="{Binding Articles}" 
                          Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                          x:Name="ArticlesListView"
                          IsItemClickEnabled="True"
                          ItemClick="ArticlesListView_OnItemClick"
                          Grid.Row="1">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0, 10, 0, 10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="125"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="125"/>
                                </Grid.RowDefinitions>

                                <Image Grid.Row="0" 
                                       Grid.Column="0" 
                                       Source="{Binding Image}" 
                                       Stretch="UniformToFill" />
                                <StackPanel Grid.Row="0" 
                                            Grid.Column="1"
                                            Padding="20, 0, 10, 0">
                                    <TextBlock Text="{Binding Title}" 
                                               FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Summary}" 
                                               TextWrapping="Wrap"
                                               TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </SplitView.Content>
    </SplitView>
</Page>































































































































